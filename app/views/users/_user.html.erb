<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
  <!-- method: :delete arranges for the necessary DELETE request to be issued
  web browsers cannot nativel send delete requests, so rails fakes them using javascript -->
  	| <%= link_to "Delete", user, method: :delete, 
  											data: { confirm: "You sure?" } %>
  <% end %>
</li>